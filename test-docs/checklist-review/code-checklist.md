# Code Checklist

<table>
  <tr><td><strong>Project Name</strong></td><td colspan="6"></td><td colspan="2" align="center"><strong>Code Checklist</strong></td></tr>
  <tr><td><strong>Reviewed Document</strong></td><td colspan="8">Code review</td></tr>
  <tr><td><strong>Reviewed Feature/Scope</strong></td><td colspan="8">Backend + Frontend only (exclude mobile), checklist items 1-60</td></tr>
  <tr><td><strong>Number of Pass</strong></td><td colspan="8">75</td></tr>
  <tr><td><strong>Number of Fail</strong></td><td colspan="8">82</td></tr>
  <tr><td><strong>Number of N/A</strong></td><td colspan="8">5</td></tr>
  <tr><td><strong>Number of Errors</strong></td><td colspan="8">76</td></tr>
</table>

<table>
  <tr>
    <th rowspan="2">No.</th>
    <th rowspan="2">Checklist Question</th>
    <th colspan="3">Review Result (Pass/Fail/N/A)</th>
    <th rowspan="2">Notes</th>
  </tr>
  <tr>
    <th>Pass</th>
    <th>Fail</th>
    <th>N/A</th>
  </tr>

  <tr><td>1</td><td>Has the coding guideline specified in the project plan been adhered to?</td><td>&#9744;</td><td>&#9745;</td><td>&#9744;</td><td>No project plan/coding guideline document was found in the reviewed backend/frontend scope, so adherence cannot be verified; `backend/package.json` also has no lint/format script to enforce a coding standard.</td></tr>
  <tr><td>2</td><td>Is inline documentation adequate?</td><td>&#9744;</td><td>&#9745;</td><td>&#9744;</td><td>`backend/src/services/task.service.js` (~3358 lines) and `frontend/app/components/views/TasksView/TaskDetailModal.tsx` (~2665 lines) contain complex logic with limited inline explanation, so inline documentation is not adequate for the current complexity.</td></tr>
  <tr><td>3</td><td>Do naming conventions conform to the configuration management plan?</td><td>&#9744;</td><td>&#9745;</td><td>&#9744;</td><td>No configuration management plan or naming convention document was found in the reviewed scope; naming is also inconsistent across source, for example model files `Task.model.js`/`User.model.js` use PascalCase while controller files such as `admin.controller.js`/`auth.controller.js` use lowercase naming without a documented convention.</td></tr>
  <tr><td>4</td><td>Has code been properly formatted?</td><td>&#9744;</td><td>&#9745;</td><td>&#9744;</td><td>Formatting/readability is inconsistent. `frontend/app/components/common/UserMenu.tsx` and `frontend/app/constants/groupRoles.ts` contain garbled UTF-8 text in comments/labels, and the backend has no formatter script/config in `backend/package.json` to enforce consistent formatting.</td></tr>
  <tr><td>5</td><td>Has a common set of routines been written without duplicating these routines in different programs?</td><td>&#9744;</td><td>&#9745;</td><td>&#9744;</td><td>Common routine duplication exists in frontend: `getInitials()` is duplicated in `frontend/app/components/common/UserMenu.tsx` and `frontend/app/components/ProfileSettings.tsx` instead of being shared as a reusable helper.</td></tr>
  <tr><td>6</td><td>Is there any redundant or trash code?</td><td>&#9744;</td><td>&#9745;</td><td>&#9744;</td><td>Redundant/debug code remains in the reviewed scope: `frontend/app/debug-api.js` is a debug-only source file, and `frontend/app/services/task.service.ts` contains extensive request/response debug `console.log` statements that are not part of core business logic.</td></tr>
  <tr><td>7</td><td>Are there any declared routes, handlers, or helper functions that are never used?</td><td>&#9744;</td><td>&#9745;</td><td>&#9744;</td><td>Unused helper found: `getRoleSummary` is exported in `frontend/app/utils/groupRoleUtils.ts` but no usage was found in the reviewed backend/frontend scope.</td></tr>
  <tr><td>8</td><td>Have pointers been set to NULL if necessary?</td><td>&#9744;</td><td>&#9744;</td><td>&#9745;</td><td>Not applicable for JavaScript/TypeScript in this backend/frontend scope because the reviewed code does not use manual pointers or NULL pointer management.</td></tr>
  <tr><td>9</td><td>Does pointer arithmetic result in pointing to memory that is out of range?</td><td>&#9744;</td><td>&#9744;</td><td>&#9745;</td><td>Not applicable for JavaScript/TypeScript in this backend/frontend scope because the reviewed code does not perform pointer arithmetic or direct memory access.</td></tr>
  <tr><td>10</td><td>Are all the array indices within bounds?</td><td>&#9744;</td><td>&#9745;</td><td>&#9744;</td><td>Array index bounds are not fully guarded in `frontend/app/services/meeting.service.ts`, where `event.streams[0]` is accessed in two `ontrack` handlers without checking that `event.streams` contains at least one stream.</td></tr>
  <tr><td>11</td><td>Are all the array indices correctly initialized?</td><td>&#9745;</td><td>&#9744;</td><td>&#9744;</td><td>No uninitialized array index variable was found in the reviewed scope; representative indexed loops initialize their counters explicitly, for example `let i = 0` in `frontend/app/components/views/TasksView/RepeatTaskModal.tsx` and `idx` normalization in `frontend/app/components/views/TasksView/EstimatedTimePicker.tsx`.</td></tr>
  <tr><td>12</td><td>Are all the branch conditions correct?</td><td>&#9744;</td><td>&#9745;</td><td>&#9744;</td><td>Incorrect branch condition found in `frontend/app/components/ProfileSettings.tsx`: the field label is generated from `t('regional.timezone')`, but the save branch checks `field.label === 'Time Zone'`, so the timezone-specific branch fails when the UI language is not English.</td></tr>
  <tr><td>13</td><td>Do all loops terminate?</td><td>&#9745;</td><td>&#9744;</td><td>&#9744;</td><td>No non-terminating loop was confirmed in the reviewed scope. Representative loops are bounded by explicit limits or progress conditions, for example `for (let i = 0; i < maxDates; i++)` in `frontend/app/components/views/TasksView/RepeatTaskModal.tsx` and the value-normalization loops in `frontend/app/components/views/TasksView/EstimatedTimePicker.tsx`.</td></tr>
  <tr><td>14</td><td>Is the condition for terminating a loop realistic?</td><td>&#9745;</td><td>&#9744;</td><td>&#9744;</td><td>Loop termination conditions appear realistic in the reviewed scope. For example, recurrence generation in `frontend/app/components/views/TasksView/RepeatTaskModal.tsx` stops on `maxDates`, `endDate`, or `occurrences`, and the normalization loops in `EstimatedTimePicker.tsx` move the values toward bounded ranges on each iteration.</td></tr>
  <tr><td>15</td><td>Have the denominators in division operations been checked for zero before performing the division?</td><td>&#9745;</td><td>&#9744;</td><td>&#9744;</td><td>No divide-by-zero defect was confirmed in the reviewed scope. Representative guards exist, for example `duration > 0 ? (currentTime / duration) * 100 : 0` in `frontend/app/components/common/VoiceMessagePlayer.tsx` and `Math.max(..., 1)` before `item.count / maxCount` in `frontend/app/admin/page.tsx`.</td></tr>
  <tr><td>16</td><td>Can any statements placed inside a loop be placed outside the loop?</td><td>&#9744;</td><td>&#9745;</td><td>&#9744;</td><td>Yes. In `frontend/app/admin/page.tsx`, `const maxCount = Math.max(...analytics.activeUsersChart.map((d: any) => d.count), 1);` is recomputed inside `analytics.activeUsersChart.map(...)` for every item even though it is invariant for the whole loop and can be moved outside.</td></tr>
  <tr><td>17</td><td>Are there any portions in the code that the thread of execution never reaches?</td><td>&#9744;</td><td>&#9745;</td><td>&#9744;</td><td>Yes. `frontend/app/utils/groupRoleUtils.ts` defines `canManageRoles = () => false`, so the `canEditRoles`-guarded edit/delete branches in `frontend/app/components/views/GroupMembersView.tsx` are effectively unreachable during execution.</td></tr>
  <tr><td>18</td><td>Are “if” statements nested to more than three levels?</td><td>&#9745;</td><td>&#9744;</td><td>&#9744;</td><td>No confirmed case over three levels was found in the reviewed backend/frontend scope. Representative complex blocks such as `assignUsersToTask()` in `backend/src/services/task.service.js` contain multiple conditions, but the confirmed nesting depth stays within three levels.</td></tr>
  <tr><td>19</td><td>Do the actual and formal interface parameters match?</td><td>&#9745;</td><td>&#9744;</td><td>&#9744;</td><td>No concrete parameter mismatch was confirmed in the reviewed scope. Spot checks on helper usage such as `canManageFolders(effectiveRole, isLeader, isPersonalOwner)` in `frontend/app/components/layouts/Sidebar.tsx` match the helper signature in `frontend/app/utils/groupRoleUtils.ts`, and the frontend linter run did not report argument-count/type mismatches.</td></tr>
  <tr><td>20</td><td>Are there any unused variables declared?</td><td>&#9744;</td><td>&#9745;</td><td>&#9744;</td><td>Yes. Running `npm run lint` in the frontend reported many unused-variable warnings, for example `setEmail` in `frontend/app/components/ProfileSettings.tsx`, `canDeleteFolders`/`canEditFolders`/`canAssignFolders` in `frontend/app/components/layouts/Sidebar.tsx`, and `Filter`/`selectedUser` in `frontend/app/admin/page.tsx`.</td></tr>
  <tr><td>21</td><td>Has the memory been correctly initialized?</td><td>&#9744;</td><td>&#9744;</td><td>&#9745;</td><td>Not applicable for JavaScript/TypeScript in this backend/frontend scope because the reviewed code does not manually allocate or initialize raw memory buffers as part of normal application logic.</td></tr>
  <tr><td>22</td><td>Has dynamic memory that has been allocated on entry been released at all exit points?</td><td>&#9744;</td><td>&#9744;</td><td>&#9745;</td><td>Not applicable for the reviewed JavaScript/TypeScript code because memory lifecycle is handled by the runtime garbage collector rather than manual deallocation at each exit point.</td></tr>
  <tr><td>23</td><td>Do MongoDB queries use appropriate indexes for frequently searched, filtered, or sorted fields?</td><td>&#9745;</td><td>&#9744;</td><td>&#9744;</td><td>Yes. The backend defines indexes for frequent access paths, for example `backend/src/models/Task.model.js` indexes `groupId + status`, `folderId + status`, `assignedTo.userId`, `dueDate`, `priority`, `createdAt`, and text search on `title/description`; `Folder`, `Group`, `Notification`, and `Note` models also define targeted indexes for their common queries.</td></tr>
  <tr><td>24</td><td>Are database operations wrapped with proper error handling (try/catch), and are query failures logged or returned consistently?</td><td>&#9745;</td><td>&#9744;</td><td>&#9744;</td><td>Yes at a practical review level. Async controllers are wrapped by `backend/src/middlewares/asyncHandler.js`, unhandled errors flow into `backend/src/middlewares/errorHandler.js`, and many explicit database/file integration failures are also caught and logged or mapped to responses, such as in `backend/src/controllers/user.controller.js` and `backend/src/services/task.service.js`.</td></tr>
  <tr><td>25</td><td>Are concurrent update risks handled appropriately (atomic update, optimistic check, transaction where needed)?</td><td>&#9744;</td><td>&#9745;</td><td>&#9744;</td><td>No. Multi-document write flows are not protected by a transaction or optimistic check in several places; for example `backend/src/services/task.service.js` links two tasks by saving `linkedTask` and `sourceTask` separately, and `backend/src/services/notification.consumer.js` accepts a group invitation by updating `group`, `notification`, and `user` in separate saves, which can leave partial state if one step fails mid-flow.</td></tr>

  <tr><td>26</td><td>Have the following conditions been checked in expressions?</td><td>&#9744;</td><td>&#9745;</td><td>&#9744;</td><td>Partially only. The reviewed scope contains both good defensive checks and concrete misses, so the overall checklist item is marked Fail; see sub-items 26a-26e below.</td></tr>
  <tr><td></td><td>a. Are null or undefined values handled before the expression is evaluated?</td><td>&#9744;</td><td>&#9745;</td><td>&#9744;</td><td>No. `frontend/app/services/meeting.service.ts` reads `event.streams[0]` in two `ontrack` handlers without first checking that `event.streams` contains a value, so the subsequent logic can evaluate against `undefined`.</td></tr>
  <tr><td></td><td>b. Are type mismatches prevented before comparison or calculation?</td><td>&#9745;</td><td>&#9744;</td><td>&#9744;</td><td>No concrete type-mismatch defect was confirmed in the reviewed scope. Representative parsing and validation paths use explicit normalization such as `parseInt(..., 10)`, `Number(...)`, role validation against enums, and request validators in `backend/src/middlewares/validator.js`.</td></tr>
  <tr><td></td><td>c. Are default or fallback values applied where missing input may occur?</td><td>&#9745;</td><td>&#9744;</td><td>&#9744;</td><td>Yes in many representative paths. Examples include `validatePagination()` defaulting page/limit in `backend/src/utils/validationHelper.js`, `languages.find(...) || languages[0]` in `frontend/app/components/AuthPages.tsx`, and multiple UI numeric fields that fall back with `parseInt(...) || 0/1`.</td></tr>
  <tr><td></td><td>d. Can the expression produce NaN, Infinity, or another invalid result?</td><td>&#9744;</td><td>&#9745;</td><td>&#9744;</td><td>Yes, there is at least one unguarded case. In `frontend/app/components/common/VoiceMessagePlayer.tsx`, `const percentage = clickX / rect.width;` does not guard against `rect.width === 0`, so the expression can produce `Infinity` or `NaN` before it is applied to `audio.currentTime`.</td></tr>
  <tr><td></td><td>e. Are user inputs or request values sanitized before being used in expressions?</td><td>&#9745;</td><td>&#9744;</td><td>&#9744;</td><td>Yes in the reviewed backend paths. Input validation and sanitization are present through `backend/src/middlewares/validator.js`, `backend/src/utils/validationHelper.js`, `express-mongo-sanitize`, and multer file filtering in `backend/src/middlewares/upload.js` before values are used in queries or calculations.</td></tr>

  <tr><td>27</td><td>Will the requirements of response time be met?</td><td>&#9744;</td><td>&#9745;</td><td>&#9744;</td><td>No based on the project’s own performance evidence. `test-docs/performance/BAO_CAO_THUC_THI_PERFORMANCE_JMETER_2026-04-22.md` states the smoke run already had `PERF-01 Login` at `2764ms` over a `2000ms` threshold, and the full run failed almost completely (`99.82%` error rate) with `Read timed out` and `Connection reset`.</td></tr>
  <tr><td>28</td><td>Is there a better alternative for improving the response times?</td><td>&#9744;</td><td>&#9745;</td><td>&#9744;</td><td>Yes. The performance report itself recommends better alternatives that are not yet reflected as completed improvements, including staged load reruns, MongoDB index review for login/task/notification/chat paths, and trimming/projection of large payloads for kanban, chat history, and admin lists.</td></tr>

  <tr><td>29</td><td>Have the following checks been performed</td><td>&#9745;</td><td>&#9744;</td><td>&#9744;</td><td>Yes at a representative level; see sub-items 29a-29b.</td></tr>
  <tr><td></td><td>a. Checks for empty query results, missing documents, or empty files/uploads?</td><td>&#9745;</td><td>&#9744;</td><td>&#9744;</td><td>Yes. Examples include `TASK_NOT_FOUND` responses in `backend/src/controllers/task.controller.js`, `GROUP_NOT_FOUND` checks in group/task services, and explicit upload guards such as `No file was uploaded` in `backend/src/controllers/user.controller.js` and `No file provided` in `backend/src/controllers/chat.controller.js`.</td></tr>
  <tr><td></td><td>b. Checks for I/O, database, file upload, or external service errors?</td><td>&#9745;</td><td>&#9744;</td><td>&#9744;</td><td>Yes. Representative examples include `backend/src/middlewares/errorHandler.js` for uncaught backend errors, `backend/src/middlewares/upload.js` for multer failures, and explicit try/catch logging around Cloudinary and notification dispatch in `backend/src/services/task.service.js` and `backend/src/services/chat.service.js`.</td></tr>

  <tr><td>30</td><td>Are the error messages clear? Are the error messages adequate?</td><td>&#9744;</td><td>&#9745;</td><td>&#9744;</td><td>Not consistently. Several source files still contain garbled UTF-8/Vietnamese text in user-facing strings, for example upload error responses in `backend/src/middlewares/upload.js` and multiple labels/messages in frontend files such as `frontend/app/components/common/UserMenu.tsx`, which reduces clarity and adequacy of displayed errors.</td></tr>
  <tr><td>31</td><td>Have all error conditions been trapped and handled?</td><td>&#9744;</td><td>&#9745;</td><td>&#9744;</td><td>No. Some runtime edge cases are still not trapped before execution, for example `frontend/app/services/meeting.service.ts` accesses `event.streams[0]` without guarding the stream array length, and `frontend/app/components/common/VoiceMessagePlayer.tsx` calculates `clickX / rect.width` without guarding zero width.</td></tr>

  <tr><td>32</td><td>In arithmetic expressions, have the following been addressed:</td><td>&#9744;</td><td>&#9745;</td><td>&#9744;</td><td>Partially only. The reviewed scope contains both well-formed arithmetic and a few readability/safety issues, so the overall item is marked Fail; see sub-items 32a-32f.</td></tr>
  <tr><td></td><td>a. Is the order of processing unambiguous?</td><td>&#9745;</td><td>&#9744;</td><td>&#9744;</td><td>Yes in the representative arithmetic checked. Expressions such as `Math.floor((seconds % 3600) / 60)` in `frontend/app/components/views/TasksView/TaskDetailModal.tsx` and `duration > 0 ? (currentTime / duration) * 100 : 0` in `frontend/app/components/common/VoiceMessagePlayer.tsx` make the precedence explicit.</td></tr>
  <tr><td></td><td>b. Is there any need for horizontal scrolling to read the entire expression?</td><td>&#9744;</td><td>&#9745;</td><td>&#9744;</td><td>Yes. Some inline arithmetic expressions are long enough to hurt readability, for example the width calculation in `frontend/app/admin/page.tsx` that nests a ternary, `Number(...)`, division, multiplication, and `Math.min(...)` inside one JSX style expression.</td></tr>
  <tr><td></td><td>c. Are all parentheses properly closed? Do they ensure proper order of processing?</td><td>&#9745;</td><td>&#9744;</td><td>&#9744;</td><td>No broken or mismatched arithmetic parentheses were confirmed in the reviewed scope, and the checked expressions use parentheses in a syntactically valid way.</td></tr>
  <tr><td></td><td>d. Is rounding off performed along with the expression?</td><td>&#9745;</td><td>&#9744;</td><td>&#9744;</td><td>Yes where rounding is materially needed. Examples include `Math.round((totalHours - hours) * 60)` in `backend/src/models/Task.model.js` and `parseFloat((bytes / Math.pow(k, i)).toFixed(2))` in `frontend/app/admin/page.tsx`.</td></tr>
  <tr><td></td><td>e. Is division joined with another expression?</td><td>&#9744;</td><td>&#9745;</td><td>&#9744;</td><td>Yes. There are combined arithmetic expressions that chain division with additional operations, for example `(currentTime / duration) * 100` in `frontend/app/components/common/VoiceMessagePlayer.tsx` and `(item.count / maxCount) * 100` in `frontend/app/admin/page.tsx`.</td></tr>
  <tr><td></td><td>f. Does any expression use table fields or file fields directly in the expression?</td><td>&#9744;</td><td>&#9744;</td><td>&#9745;</td><td>Not applicable as written for this JavaScript/TypeScript web application review; the checklist wording assumes table/file-field style procedural systems rather than the current object/document model.</td></tr>

  <tr><td>33</td><td>In relational expressions, have the following been addressed</td><td>&#9744;</td><td>&#9745;</td><td>&#9744;</td><td>Partially only. Several relational checks are sound, but there are also concrete purpose/readability issues; see sub-items 33a-33d.</td></tr>
  <tr><td></td><td>a. Are comparisons between the same types of data?</td><td>&#9745;</td><td>&#9744;</td><td>&#9744;</td><td>No concrete same-type comparison defect was confirmed in the reviewed scope. Representative checks normalize IDs with `toString()` and use strict comparisons such as `===`/`!==` in task and group services.</td></tr>
  <tr><td></td><td>b. Is it possible to have more than two outcomes for any expression ?</td><td>&#9745;</td><td>&#9744;</td><td>&#9744;</td><td>The checked relational expressions themselves evaluate to boolean outcomes only; no confirmed tri-state relational defect was found.</td></tr>
  <tr><td></td><td>c.Does the expression serve the purpose for which it is used?</td><td>&#9744;</td><td>&#9745;</td><td>&#9744;</td><td>No, not always. In `frontend/app/components/ProfileSettings.tsx`, the comparison `field.label === 'Time Zone'` does not reliably serve its intended purpose once labels are translated, so the timezone branch becomes logically incorrect for non-English UI.</td></tr>
  <tr><td></td><td>d. Is there any need for horizontal scrolling to read the entire expression?</td><td>&#9744;</td><td>&#9745;</td><td>&#9744;</td><td>Yes. Some relational checks are embedded inside long JSX or compound service conditions, which reduces scanability, for example the long folder/task comparisons in `backend/src/services/task.service.js` and inline view expressions in frontend components.</td></tr>

  <tr><td>34</td><td>In logical expressions, have the following been addressed</td><td>&#9744;</td><td>&#9745;</td><td>&#9744;</td><td>Partially only. The codebase contains many valid boolean checks, but there are still purpose/readability issues and multi-clause conditions that do not meet the stricter checklist style; see sub-items 34a-34e.</td></tr>
  <tr><td></td><td>a. Does the logical expression serve the purpose for which it is used?</td><td>&#9744;</td><td>&#9745;</td><td>&#9744;</td><td>Not always. The translated-label logic in `frontend/app/components/ProfileSettings.tsx` is a concrete case where the logical branch no longer serves the intended business purpose under non-English labels.</td></tr>
  <tr><td></td><td>b. Does each relational expression used result in a true or false outcome ?</td><td>&#9745;</td><td>&#9744;</td><td>&#9744;</td><td>Yes in the representative logical expressions reviewed. Conditions use standard JS booleans and strict comparisons such as `===`, `!==`, `.includes(...)`, and explicit truthiness checks.</td></tr>
  <tr><td></td><td>c. Is each relational expression inside a set of parentheses?</td><td>&#9744;</td><td>&#9745;</td><td>&#9744;</td><td>No. Multi-part expressions often rely on operator precedence without wrapping each relation, for example `if (sourceTask.folderId && linkedTask.folderId && sourceTask.folderId.toString() !== linkedTask.folderId.toString())` in `backend/src/services/task.service.js`.</td></tr>
  <tr><td></td><td>d. At any given time, are only two relational expressions compared?</td><td>&#9744;</td><td>&#9745;</td><td>&#9744;</td><td>No. Several logical conditions chain more than two relational/truthy checks at once, such as the folder-link validation and permission checks in `backend/src/services/task.service.js`.</td></tr>
  <tr><td></td><td>e. Is there any need for horizontal scrolling to read the entire expression?</td><td>&#9744;</td><td>&#9745;</td><td>&#9744;</td><td>Yes. Some logical expressions become long enough to reduce readability, especially in `backend/src/services/task.service.js` and large frontend JSX conditionals.</td></tr>

  <tr><td>35</td><td>Have resource handling and cleanup been implemented properly?</td><td>&#9744;</td><td>&#9745;</td><td>&#9744;</td><td>Partially only. Many listeners/timers/sockets are cleaned up correctly, but not all transient resources are consistently released; see sub-items 35a-35b.</td></tr>
  <tr><td></td><td>a. Are database connections, file streams, sockets, or external resources opened only when they are actually needed?</td><td>&#9745;</td><td>&#9744;</td><td>&#9744;</td><td>Yes in the representative paths checked. Examples include on-demand socket setup/teardown in frontend hooks, authenticated route usage, and backend service calls that open external integrations only within the relevant request flow.</td></tr>
  <tr><td></td><td>b. Are these resources properly closed, released, or cleaned up after the operations are completed?</td><td>&#9744;</td><td>&#9745;</td><td>&#9744;</td><td>Not consistently. Many cleanups exist, but `frontend/app/components/ProfileSettings.tsx` creates `avatarPreview` with `URL.createObjectURL(file)` and no corresponding `URL.revokeObjectURL(...)` was found, unlike the cleanup implemented in `NotesView.tsx` and `TaskDetailModal.tsx`.</td></tr>

  <tr><td>36</td><td>In variable declarations, have the following been addressed:</td><td>&#9744;</td><td>&#9745;</td><td>&#9744;</td><td>Partially only. There are clear issues with unnecessary declarations, naming consistency, and hard-coded values; see sub-items 36a-36d.</td></tr>
  <tr><td></td><td>a. Do all the variables declared as global or static really need to be global or static?</td><td>&#9745;</td><td>&#9744;</td><td>&#9744;</td><td>No concrete misuse of global/static declarations was confirmed in the reviewed backend/frontend scope. Most top-level declarations are module-scoped constants or configuration values.</td></tr>
  <tr><td></td><td>b. Are there any declarations of unnecessary variables?</td><td>&#9744;</td><td>&#9745;</td><td>&#9744;</td><td>Yes. Frontend linting reported many unnecessary declarations, for example `setEmail` in `frontend/app/components/ProfileSettings.tsx`, `canDeleteFolders`/`canEditFolders`/`canAssignFolders` in `frontend/app/components/layouts/Sidebar.tsx`, and `selectedUser` in `frontend/app/admin/page.tsx`.</td></tr>
  <tr><td></td><td>c. Do variable, function, class, and route names avoid reserved keywords and remain clear, consistent, and domain-meaningful?</td><td>&#9744;</td><td>&#9745;</td><td>&#9744;</td><td>Not consistently. The reviewed scope already shows naming inconsistency such as `Task.model.js`/`User.model.js` versus lowercase controller files, and some names are generic or mixed-language rather than consistently domain-standardized.</td></tr>
  <tr><td></td><td>d. Is there any hard coding inside the code?</td><td>&#9744;</td><td>&#9745;</td><td>&#9744;</td><td>Yes. Hard-coded values remain in several places, for example the default super-admin credentials in `backend/src/utils/initSuperAdmin.js`, hard-coded localhost fallback URLs in frontend components/services, and literal labels such as `'Time Zone'` in `ProfileSettings.tsx`.</td></tr>

  <tr><td>37</td><td>Do controllers remain thin and delegate business logic to service or utility layers?</td><td>&#9745;</td><td>&#9744;</td><td>&#9744;</td><td>Yes overall. Representative controllers such as `backend/src/controllers/task.controller.js` mainly extract request data, perform lightweight response handling, and delegate business logic to services like `taskService`.</td></tr>
  <tr><td>38</td><td>Have route handlers been organized consistently according to the project structure?</td><td>&#9745;</td><td>&#9744;</td><td>&#9744;</td><td>Yes overall. The backend follows a consistent `routes -> controllers -> services/middlewares` structure across task, group, admin, chat, and user flows.</td></tr>

  <tr><td>39</td><td>Are request inputs validated before being processed?</td><td>&#9744;</td><td>&#9745;</td><td>&#9744;</td><td>Partially only. The project has substantial validation support, but it is not applied consistently at the route boundary for every input path; see sub-items 39a-39d.</td></tr>
  <tr><td></td><td>a. Are req.body fields validated?</td><td>&#9744;</td><td>&#9745;</td><td>&#9744;</td><td>Not consistently at the route layer. Some bodies are validated through middleware such as `validateCreateTask`, but other body-driven routes like `POST /api/tasks/:id/link`, `POST /api/tasks/:id/repeat`, and `POST /api/tasks/:id/custom-status` in `backend/src/routes/task.routes.js` do not apply dedicated request-body validator middleware before reaching the controller/service.</td></tr>
  <tr><td></td><td>b. Are req.params values validated?</td><td>&#9744;</td><td>&#9745;</td><td>&#9744;</td><td>Not consistently at the route boundary. Several param-based routes such as `GET /api/tasks/:id`, `POST /api/groups/:id/join`, and `GET /api/groups/:id/tasks` do not use explicit param validator middleware before controller execution.</td></tr>
  <tr><td></td><td>c. Are req.query values validated?</td><td>&#9744;</td><td>&#9745;</td><td>&#9744;</td><td>Not consistently before processing. Query-heavy endpoints rely on service-level sanitation in some places, but there is no uniform query-validator middleware applied across list/search endpoints at the route layer.</td></tr>
  <tr><td></td><td>d. Are invalid ObjectId values rejected safely?</td><td>&#9745;</td><td>&#9744;</td><td>&#9744;</td><td>Yes in the representative backend paths reviewed. Invalid IDs are commonly rejected via `isValidObjectId(...)` checks in services and through Mongoose/CastError handling in `backend/src/middlewares/errorHandler.js`.</td></tr>

  <tr><td>40</td><td>Are authorization and access control checks implemented correctly?</td><td>&#9745;</td><td>&#9744;</td><td>&#9744;</td><td>Yes overall in the reviewed backend scope. Authentication and access control are enforced through middleware and service-level permission checks; see sub-items 40a-40c.</td></tr>
  <tr><td></td><td>a. Is authentication required on protected routes?</td><td>&#9745;</td><td>&#9744;</td><td>&#9744;</td><td>Yes. Protected route modules such as `backend/src/routes/task.routes.js`, `group.routes.js`, and `admin.routes.js` apply `authenticate` before handling requests.</td></tr>
  <tr><td></td><td>b. Are role-based checks enforced for admin-only features?</td><td>&#9745;</td><td>&#9744;</td><td>&#9744;</td><td>Yes. `backend/src/routes/admin.routes.js` applies `requireAdmin`, and admin-role management endpoints additionally require `canManageAdmins` for super-admin-only operations.</td></tr>
  <tr><td></td><td>c. Are group-, folder-, or task-scoped permissions checked before access?</td><td>&#9745;</td><td>&#9744;</td><td>&#9744;</td><td>Yes overall. Examples include `requireCurrentGroup` in task routes, membership checks in `backend/src/middlewares/groupAuth.js`, and task/folder-specific permission checks inside services such as `task.service.js`.</td></tr>
  <tr><td></td><td>d. Is access denied by default when permission is unclear or missing?</td><td>&#9745;</td><td>&#9744;</td><td>&#9744;</td><td>Yes in the representative backend access-control paths reviewed. Missing authentication or insufficient role/group context returns `401`/`403` through middlewares such as `authenticate`, `requireAdmin`, `canManageAdmins`, and `requireCurrentGroup` rather than allowing the request to proceed.</td></tr>

  <tr><td>41</td><td>Are API responses structured consistently for success and error cases?</td><td>&#9744;</td><td>&#9745;</td><td>&#9744;</td><td>Partially only. The backend has shared `sendSuccess`/`sendError` helpers in `backend/src/utils/response.js`, but internal details are still exposed in some error paths, so response handling is not consistently safe across all API outcomes.</td></tr>
  <tr><td></td><td>a. Do success responses follow a consistent structure?</td><td>&#9745;</td><td>&#9744;</td><td>&#9744;</td><td>Yes overall. Controllers consistently use `sendSuccess(res, data, message, statusCode)` from `backend/src/utils/response.js`, which returns the same `{ success, message, data }` envelope.</td></tr>
  <tr><td></td><td>b. Do error responses follow a consistent structure?</td><td>&#9745;</td><td>&#9744;</td><td>&#9744;</td><td>Yes overall. `sendError` and `backend/src/middlewares/errorHandler.js` both return a stable `{ success: false, message }` shape with optional `errors` metadata.</td></tr>
  <tr><td></td><td>c. Are HTTP status codes used appropriately?</td><td>&#9745;</td><td>&#9744;</td><td>&#9744;</td><td>Yes in the representative backend paths reviewed. The code distinguishes `400`, `401`, `403`, `404`, `409`, and `500` in controllers/middlewares such as `auth.js`, `groupAuth.js`, and `errorHandler.js`.</td></tr>
  <tr><td></td><td>d. Are internal error details hidden from end users?</td><td>&#9744;</td><td>&#9745;</td><td>&#9744;</td><td>No. `backend/src/middlewares/errorHandler.js` adds `response.stack = err.stack` in development, and `backend/src/middlewares/auth.js` returns raw token-verification error messages via `sendError(res, error.message, 401)`, exposing internal detail instead of consistently hiding it.</td></tr>

  <tr><td>42</td><td>Has error handling been implemented consistently across asynchronous operations?</td><td>&#9745;</td><td>&#9744;</td><td>&#9744;</td><td>Yes overall. Async controllers are wrapped with `asyncHandler`, socket handlers generally use local `try/catch`, and backend services propagate failures through explicit exceptions or structured result objects.</td></tr>
  <tr><td></td><td>a. Are async route handlers wrapped safely?</td><td>&#9745;</td><td>&#9744;</td><td>&#9744;</td><td>Yes. Reviewed controllers such as `task.controller.js`, `group.controller.js`, `notification.controller.js`, and `user.controller.js` wrap async handlers with `asyncHandler(...)`.</td></tr>
  <tr><td></td><td>b. Are rejected promises handled properly?</td><td>&#9745;</td><td>&#9744;</td><td>&#9744;</td><td>Yes overall. `asyncHandler` forwards promise rejections to Express error middleware, and socket/service code commonly catches asynchronous failures explicitly before replying or logging.</td></tr>
  <tr><td></td><td>c. Are custom errors distinguished from unexpected system errors?</td><td>&#9745;</td><td>&#9744;</td><td>&#9744;</td><td>Yes in the reviewed backend scope. The project distinguishes validation, cast, duplicate-key, JWT, and explicit `statusCode`-based domain errors in `backend/src/middlewares/errorHandler.js` and service helpers such as `buildError`/`raiseError`.</td></tr>
  <tr><td></td><td>d. Are errors logged at appropriate points?</td><td>&#9745;</td><td>&#9744;</td><td>&#9744;</td><td>Yes overall. Representative failures are logged in middleware and services such as `errorHandler.js`, `file.service.js`, `notification.consumer.js`, and realtime/socket handlers.</td></tr>

  <tr><td>43</td><td>Has duplicate business logic been avoided across controllers, services, and utilities?</td><td>&#9744;</td><td>&#9745;</td><td>&#9744;</td><td>No. Similar chat business flows are duplicated across the group-chat and direct-chat stacks, for example parallel create/edit/delete/reaction/upload logic in `backend/src/controllers/chat.controller.js` and `backend/src/controllers/directChat.controller.js` with matching service layers `chat.service.js` and `directChat.service.js`, increasing maintenance risk.</td></tr>

  <tr><td>44</td><td>Are secrets and environment-specific values handled safely?</td><td>&#9744;</td><td>&#9745;</td><td>&#9744;</td><td>No. The environment config still contains insecure hard-coded fallback secrets and permissive environment fallbacks; see `backend/src/config/environment.js`.</td></tr>
  <tr><td></td><td>a. Are secrets excluded from source code?</td><td>&#9744;</td><td>&#9745;</td><td>&#9744;</td><td>No. `backend/src/config/environment.js` embeds fallback secrets such as `default_secret_change_this` and `default_refresh_secret_change_this` directly in source code.</td></tr>
  <tr><td></td><td>b. Are environment variables used instead of hard-coded credentials?</td><td>&#9744;</td><td>&#9745;</td><td>&#9744;</td><td>Not consistently. The JWT configuration in `backend/src/config/environment.js` falls back to hard-coded credential strings when env vars are missing, instead of failing closed.</td></tr>
  <tr><td></td><td>c. Are fallback values safe and appropriate?</td><td>&#9744;</td><td>&#9745;</td><td>&#9744;</td><td>No. Unsafe defaults remain, including `mongodb://localhost:27017/todolist`, hard-coded JWT fallback secrets, and permissive production-origin fallbacks in `backend/src/app.js` / `backend/src/realtime/server.js` that can allow all origins when explicit config is absent.</td></tr>

  <tr><td>45</td><td>Are MongoDB and Mongoose queries designed safely and efficiently?</td><td>&#9744;</td><td>&#9745;</td><td>&#9744;</td><td>Partially only. Indexes and pagination exist in many places, but several task queries still fetch and populate much more data than required, which weakens efficiency.</td></tr>
  <tr><td></td><td>a. Are frequently filtered fields indexed appropriately?</td><td>&#9745;</td><td>&#9744;</td><td>&#9744;</td><td>Yes overall. Representative indexes exist on frequently filtered fields such as `Task.folderId`, `Folder.groupId`, `Group.members.userId`, `Notification.recipient`, and `Note.groupId/folderId`.</td></tr>
  <tr><td></td><td>b. Are queries limited to only required fields where possible?</td><td>&#9744;</td><td>&#9745;</td><td>&#9744;</td><td>No. Several high-traffic task queries return broad documents instead of restricting fields, for example `backend/src/services/task.service.js` repeatedly fetches full task documents and only uses `populate(...)` without a matching top-level `.select(...)` for list/detail payload minimization.</td></tr>
  <tr><td></td><td>c. Is unnecessary populate usage avoided?</td><td>&#9744;</td><td>&#9745;</td><td>&#9744;</td><td>No. `backend/src/services/task.service.js` contains many heavy populate chains such as `createdBy`, `assignedTo.userId`, `comments.user`, `timeEntries.user`, `scheduledWork.user`, `activeTimers.userId`, `linkedTasks.*`, and `groupId` even on flows where only part of that data is required.</td></tr>
  <tr><td></td><td>d. Are full-collection scans avoided where possible?</td><td>&#9745;</td><td>&#9744;</td><td>&#9744;</td><td>Yes in the representative paths checked. Most reviewed queries are scoped by `groupId`, `userId`, `recipient`, or indexed identifiers instead of scanning unrestricted collections.</td></tr>
  <tr><td></td><td>e. Are pagination limits applied to list endpoints?</td><td>&#9745;</td><td>&#9744;</td><td>&#9744;</td><td>Yes overall. Representative list flows in `task.service.js`, `group.service.js`, `note.service.js`, and `admin.service.js` apply `page/limit` plus `.skip(...).limit(...)` or validated pagination helpers.</td></tr>

  <tr><td>46</td><td>Are schema definitions and model constraints aligned with business rules?</td><td>&#9744;</td><td>&#9745;</td><td>&#9744;</td><td>Partially only. Core schemas define many required fields and constraints correctly, but some role-related behavior in the group model is misaligned with the intended business rules.</td></tr>
  <tr><td></td><td>a. Are required fields marked correctly?</td><td>&#9745;</td><td>&#9744;</td><td>&#9744;</td><td>Yes in the representative models reviewed. Required fields are defined for entities such as `User.email/name/password`, `Task.title/createdBy/groupId`, `Folder.name/groupId/createdBy`, and `Notification.recipient/title/message`.</td></tr>
  <tr><td></td><td>b. Are enums and defaults defined correctly?</td><td>&#9744;</td><td>&#9745;</td><td>&#9744;</td><td>Not fully. `backend/src/models/Group.model.js` still exposes role helper methods `isProductOwner()` and `isAdmin()` that always return `false`, which conflicts with the surrounding role-based business model and makes the implemented defaults/role semantics unreliable.</td></tr>
  <tr><td></td><td>c. Are unique fields enforced where required?</td><td>&#9745;</td><td>&#9744;</td><td>&#9744;</td><td>Yes in the representative cases reviewed. Examples include `User.email` as unique and `Folder` unique indexes on `{ groupId, name }` and the default-folder constraint.</td></tr>
  <tr><td></td><td>d. Are timestamps or audit fields used consistently?</td><td>&#9745;</td><td>&#9744;</td><td>&#9744;</td><td>Yes overall. Major models such as `User`, `Task`, `Group`, `Folder`, `Note`, and `Notification` use timestamps or equivalent audit fields consistently.</td></tr>

  <tr><td>47</td><td>Are update and delete operations protected against invalid or unsafe behavior?</td><td>&#9744;</td><td>&#9745;</td><td>&#9744;</td><td>Partially only. Missing-document and permission handling are generally present, but validation is not enforced consistently on partial updates before persistence.</td></tr>
  <tr><td></td><td>a. Are missing documents handled correctly?</td><td>&#9745;</td><td>&#9744;</td><td>&#9744;</td><td>Yes overall. Representative services/controllers return `not found` results when updates or deletes target absent records, such as `task.service.js`, `note.service.js`, `user.service.js`, and `notification.consumer.js`.</td></tr>
  <tr><td></td><td>b. Are partial updates validated before persistence?</td><td>&#9744;</td><td>&#9745;</td><td>&#9744;</td><td>Not consistently. Some updates use `runValidators: true`, but others do not, for example `backend/src/services/user.service.js` updates notification settings and deactivation state with `findByIdAndUpdate(..., { new: true })` without enabling schema validators.</td></tr>
  <tr><td></td><td>c. Are dangerous mass updates or deletes avoided?</td><td>&#9745;</td><td>&#9744;</td><td>&#9744;</td><td>Yes in the representative reviewed flows. Bulk updates/deletes are scoped to explicit group/task/user filters, such as `Task.updateMany({ groupId, 'assignedTo.userId': memberId }, ...)` and cascade deletes bounded by `groupId` or `folderId`.</td></tr>
  <tr><td></td><td>d. Are ownership or permission checks performed before modification?</td><td>&#9745;</td><td>&#9744;</td><td>&#9744;</td><td>Yes overall. Representative mutations in `task.service.js`, `group.service.js`, `folder.service.js`, and note/task controllers check membership, role, or ownership before modifying data.</td></tr>

  <tr><td>48</td><td>Have concurrency-sensitive operations been handled appropriately?</td><td>&#9744;</td><td>&#9745;</td><td>&#9744;</td><td>No. Several multi-step write paths remain vulnerable to race conditions and partial success because they rely on read-modify-save flows without atomic protection or transactions.</td></tr>
  <tr><td></td><td>a. Are race conditions considered in assignment, notification, or status updates?</td><td>&#9744;</td><td>&#9745;</td><td>&#9744;</td><td>No. `backend/src/services/task.service.js` timer start/stop loads a task, mutates `activeTimers`/`timeEntries`, and saves it back, so concurrent requests can overwrite each other or bypass duplicate-timer checks.</td></tr>
  <tr><td></td><td>b. Are atomic updates used where appropriate?</td><td>&#9744;</td><td>&#9745;</td><td>&#9744;</td><td>Not consistently. Concurrency-sensitive flows such as timer mutations and linked-task updates in `backend/src/services/task.service.js` use document mutation plus `save()` instead of atomic operators like `$push`, `$pull`, conditional updates, or version-guarded writes.</td></tr>
  <tr><td></td><td>c. Are transactions used where multiple dependent writes must succeed together?</td><td>&#9744;</td><td>&#9745;</td><td>&#9744;</td><td>No. Multi-write operations such as invitation acceptance in `backend/src/services/notification.consumer.js` update `group`, `notification`, and `user` separately, and linked-task synchronization in `task.service.js` also performs dependent writes without a transaction.</td></tr>

  <tr><td>49</td><td>Are file upload and attachment operations handled safely?</td><td>&#9744;</td><td>&#9745;</td><td>&#9744;</td><td>Partially only. File type/size controls and filename sanitization exist, but upload-failure handling is not wired consistently across upload routes.</td></tr>
  <tr><td></td><td>a. Are file type checks implemented?</td><td>&#9745;</td><td>&#9744;</td><td>&#9744;</td><td>Yes. `backend/src/middlewares/upload.js` restricts uploads to explicit image, document, archive, and audio MIME types through `fileFilter`.</td></tr>
  <tr><td></td><td>b. Are file size limits enforced?</td><td>&#9745;</td><td>&#9744;</td><td>&#9744;</td><td>Yes. `backend/src/middlewares/upload.js` applies `limits.fileSize = maxFileSize` and `limits.files = 5`.</td></tr>
  <tr><td></td><td>c. Are upload failures handled correctly?</td><td>&#9744;</td><td>&#9745;</td><td>&#9744;</td><td>Not consistently. `backend/src/routes/chat.routes.js` correctly attaches `handleMulterError`, but upload routes such as `backend/src/routes/task.routes.js` (`/:id/attachments`, `/:id/comments/with-file`) and `backend/src/routes/user.routes.js` (`/me/avatar/upload`) apply multer middleware without the dedicated upload error handler.</td></tr>
  <tr><td></td><td>d. Are unsafe file names or paths sanitized?</td><td>&#9745;</td><td>&#9744;</td><td>&#9744;</td><td>Yes overall. `backend/src/services/file.service.js` sanitizes filenames before constructing Cloudinary public IDs through `sanitizeFilename(...)`, avoiding unsafe path fragments.</td></tr>

  <tr><td>50</td><td>Are real-time Socket.IO events implemented consistently and securely?</td><td>&#9744;</td><td>&#9745;</td><td>&#9744;</td><td>Partially only. Event naming and listener cleanup are mostly reasonable, but some room-join and meeting notification flows lack proper authorization checks.</td></tr>
  <tr><td></td><td>a. Are event names clear and consistent?</td><td>&#9745;</td><td>&#9744;</td><td>&#9744;</td><td>Yes overall. Reviewed realtime events use readable namespaces such as `notifications:new`, `folders:update`, `chat:message`, `direct:message`, `meeting:join`, and `presence:update`.</td></tr>
  <tr><td></td><td>b. Are room join and leave operations validated?</td><td>&#9744;</td><td>&#9745;</td><td>&#9744;</td><td>Not consistently. `backend/src/services/chat.socket.js` validates membership before `chat:join`/`direct:join`, but `backend/src/services/meeting.socket.js` allows `meeting:join` to join a group/direct meeting room based only on IDs in the payload without verifying that the caller belongs to that group or conversation.</td></tr>
  <tr><td></td><td>c. Are permission checks applied before emitting sensitive events?</td><td>&#9744;</td><td>&#9745;</td><td>&#9744;</td><td>No. In `backend/src/services/meeting.socket.js`, the first participant can trigger `meeting:incoming-call` emissions to group members or direct participants after only looking up `groupId`/`conversationId`, without first enforcing the caller's authorization for that target.</td></tr>
  <tr><td></td><td>d. Are duplicate listeners avoided?</td><td>&#9745;</td><td>&#9744;</td><td>&#9744;</td><td>Yes in the representative paths reviewed. Socket listeners are registered once per connection scope, and frontend consumers such as `NotificationDropdown.tsx` and `FolderContext.tsx` attach one named handler per event.</td></tr>
  <tr><td></td><td>e. Are listeners cleaned up properly when no longer needed?</td><td>&#9745;</td><td>&#9744;</td><td>&#9744;</td><td>Yes overall. Representative frontend listeners call `socket.off(...)`, `removeEventListener(...)`, or similar cleanup in `NotificationDropdown.tsx`, `FolderContext.tsx`, `VoiceMessagePlayer.tsx`, and other reviewed hooks/components.</td></tr>

  <tr><td>51</td><td>Are notification and messaging flows implemented reliably?</td><td>&#9744;</td><td>&#9745;</td><td>&#9744;</td><td>Partially only. Recipient scoping and duplicate consolidation exist, but unread-count and reconnect handling are not reliable across the reviewed frontend/backend notification flow.</td></tr>
  <tr><td></td><td>a. Are notifications sent only to authorized recipients?</td><td>&#9745;</td><td>&#9744;</td><td>&#9744;</td><td>Yes overall. Notification queries in `backend/src/services/notification.service.js` are scoped by `recipient: userId`, and realtime delivery in `backend/src/realtime/server.js` emits to `user:${recipientId}` rooms rather than broadcasting globally.</td></tr>
  <tr><td></td><td>b. Are unread counts updated consistently?</td><td>&#9744;</td><td>&#9745;</td><td>&#9744;</td><td>No. `frontend/app/components/NotificationDropdown.tsx` registers a socket listener that depends only on `socket` but reads `notifications.some(...)` from a stale closure, so duplicate detection and unread counts can drift after subsequent state updates.</td></tr>
  <tr><td></td><td>c. Are offline and reconnect scenarios handled appropriately?</td><td>&#9744;</td><td>&#9745;</td><td>&#9744;</td><td>Not fully. `frontend/app/hooks/useSocket.ts` reconnects the socket, but `NotificationDropdown.tsx` only reloads notifications on mount and on new-event receipt, so missed notifications/unread state are not resynchronized automatically after reconnect.</td></tr>
  <tr><td></td><td>d. Are duplicate notifications prevented where possible?</td><td>&#9745;</td><td>&#9744;</td><td>&#9744;</td><td>Yes overall. `backend/src/services/notification.consumer.js` consolidates duplicate `chat_message` and `comment_added` notifications with `findOneAndUpdate(...)` plus `messageCount` instead of always inserting a new row.</td></tr>

  <tr><td>52</td><td>Are logs and audit-related operations handled appropriately?</td><td>&#9744;</td><td>&#9745;</td><td>&#9744;</td><td>Partially only. Important actions are logged, but sensitive credentials/tokens are still written to logs in the reviewed backend/frontend auth setup.</td></tr>
  <tr><td></td><td>a. Are important security or admin actions logged?</td><td>&#9745;</td><td>&#9744;</td><td>&#9744;</td><td>Yes. `backend/src/services/auth.service.js` writes `LoginHistory` entries for success/failure/blocked logins, and `backend/src/services/admin.service.js` records admin operations through `logAdminAction(...)`.</td></tr>
  <tr><td></td><td>b. Are sensitive values excluded from logs?</td><td>&#9744;</td><td>&#9745;</td><td>&#9744;</td><td>No. Sensitive data is still logged, for example `backend/src/utils/initSuperAdmin.js` prints the default super-admin password, `frontend/app/contexts/AuthContext.tsx` logs the access token after login, and `frontend/app/services/api.client.ts` logs the full request config that includes the `Authorization` header.</td></tr>
  <tr><td></td><td>c. Are log messages clear enough for debugging and tracing?</td><td>&#9745;</td><td>&#9744;</td><td>&#9744;</td><td>Yes overall. Reviewed logs in auth, admin, realtime, and API-client flows are explicit enough to trace which action/request failed or succeeded.</td></tr>

  <tr><td>53</td><td>Are authentication and session-related flows implemented securely?</td><td>&#9744;</td><td>&#9745;</td><td>&#9744;</td><td>Partially only. Token validation and inactive-account blocking are present, but refresh-token handling is not implemented safely.</td></tr>
  <tr><td></td><td>a. Are access tokens validated correctly?</td><td>&#9745;</td><td>&#9744;</td><td>&#9744;</td><td>Yes overall. `backend/src/middlewares/auth.js` and socket authentication paths validate access tokens through `authService.verifyToken()` before allowing protected access.</td></tr>
  <tr><td></td><td>b. Are refresh token operations handled safely?</td><td>&#9744;</td><td>&#9745;</td><td>&#9744;</td><td>No. Refresh tokens are stored in plaintext in both the backend and frontend, for example `User.refreshToken` is persisted directly by `backend/src/services/auth.service.js` and `frontend/app/services/auth.service.ts` stores the refresh token in `localStorage` instead of using a safer httpOnly-cookie or hashed-token approach.</td></tr>
  <tr><td></td><td>c. Are locked or inactive accounts blocked correctly?</td><td>&#9745;</td><td>&#9744;</td><td>&#9744;</td><td>Yes. `backend/src/services/auth.service.js` blocks inactive users during login/refresh, and `backend/src/middlewares/auth.js` also refuses authenticated requests when the account is inactive.</td></tr>
  <tr><td></td><td>d. Are password-related operations protected and validated properly?</td><td>&#9745;</td><td>&#9744;</td><td>&#9744;</td><td>Yes overall. Backend auth logic enforces password-strength validation, password-change flows require the old password, and reset-code verification exists across the auth service and frontend auth pages.</td></tr>

  <tr><td>54</td><td>Are frontend service calls and UI logic separated appropriately?</td><td>&#9744;</td><td>&#9745;</td><td>&#9744;</td><td>Partially only. Service modules exist, but API orchestration and mixed UI/business responsibilities are still embedded in large frontend components.</td></tr>
  <tr><td></td><td>a. Is API calling logic kept out of purely presentational components?</td><td>&#9744;</td><td>&#9745;</td><td>&#9744;</td><td>No. `frontend/app/components/AuthPages.tsx` directly orchestrates auth-service requests in the UI component, and `frontend/app/components/ProfileSettings.tsx` performs avatar upload through a direct `fetch(...)` call instead of delegating all remote operations to a service/helper layer.</td></tr>
  <tr><td></td><td>b. Are reusable calls placed in service or utility modules?</td><td>&#9745;</td><td>&#9744;</td><td>&#9744;</td><td>Yes overall. Reusable API calls are centralized in modules such as `frontend/app/services/auth.service.ts`, `task.service.ts`, `notification.service.ts`, and `api.client.ts`.</td></tr>
  <tr><td></td><td>c. Are large components split when they contain too much mixed responsibility?</td><td>&#9744;</td><td>&#9745;</td><td>&#9744;</td><td>No. Large components such as `frontend/app/components/layouts/Sidebar.tsx` and `frontend/app/components/AuthPages.tsx` still combine substantial UI rendering, permission logic, state coordination, and request orchestration in one place.</td></tr>

  <tr><td>55</td><td>Are loading, empty, and error states handled properly in the frontend?</td><td>&#9744;</td><td>&#9745;</td><td>&#9744;</td><td>Partially only. Loading and empty states are commonly present, but some request failures are only logged and do not expose a recoverable user-facing error state.</td></tr>
  <tr><td></td><td>a. Is a loading state shown during async fetches?</td><td>&#9745;</td><td>&#9744;</td><td>&#9744;</td><td>Yes overall. Reviewed views such as `AuthPages.tsx`, `Sidebar.tsx`, and `frontend/app/admin/page.tsx` show loading indicators or disabled states while async work is in progress.</td></tr>
  <tr><td></td><td>b. Is an empty state shown when no data exists?</td><td>&#9745;</td><td>&#9744;</td><td>&#9744;</td><td>Yes overall. Representative frontend views include explicit empty states, for example folder/task sections in `Sidebar.tsx` and task-list areas in the reviewed task views.</td></tr>
  <tr><td></td><td>c. Is an error state shown when requests fail?</td><td>&#9744;</td><td>&#9745;</td><td>&#9744;</td><td>Not consistently. `frontend/app/components/NotificationDropdown.tsx` catches notification-load failures with `console.error(...)` only, so the user receives no visible error state for that failed request path.</td></tr>
  <tr><td></td><td>d. Can the user recover from the error state where appropriate?</td><td>&#9744;</td><td>&#9745;</td><td>&#9744;</td><td>Not consistently. In failure paths like notification loading, the reviewed UI offers no retry or resync action, leaving recovery dependent on a full page reload or a future socket event.</td></tr>

  <tr><td>56</td><td>Is frontend state management implemented cleanly and safely?</td><td>&#9744;</td><td>&#9745;</td><td>&#9744;</td><td>Mostly structured, but not fully safe. Contexts/hooks are used well overall, yet stale-state handling is still broken in at least one async notification flow.</td></tr>
  <tr><td></td><td>a. Is shared state placed in appropriate context or hooks?</td><td>&#9745;</td><td>&#9744;</td><td>&#9744;</td><td>Yes. Shared frontend state is centralized through contexts/hooks such as `AuthContext`, `FolderContext`, `LanguageContext`, `RegionalContext`, and `useSocket`.</td></tr>
  <tr><td></td><td>b. Is unnecessary prop drilling avoided?</td><td>&#9745;</td><td>&#9744;</td><td>&#9744;</td><td>Yes overall. Cross-cutting state like auth, folders, language, and regional settings is provided through context rather than being passed deeply through props.</td></tr>
  <tr><td></td><td>c. Are side effects isolated appropriately in hooks or effect handlers?</td><td>&#9745;</td><td>&#9744;</td><td>&#9744;</td><td>Yes overall. The reviewed frontend commonly isolates subscriptions, persistence, and fetch side effects inside `useEffect` blocks and dedicated hooks/contexts.</td></tr>
  <tr><td></td><td>d. Are stale state and race conditions considered in async UI updates?</td><td>&#9744;</td><td>&#9745;</td><td>&#9744;</td><td>No. `frontend/app/components/NotificationDropdown.tsx` uses socket callbacks that read captured `notifications` state rather than guaranteed-latest state, so async notification updates can become inconsistent.</td></tr>

  <tr><td>57</td><td>Are role-based UI restrictions implemented consistently on the frontend?</td><td>&#9745;</td><td>&#9744;</td><td>&#9744;</td><td>Yes overall in the reviewed frontend/backend scope. Role-based controls are hidden in the UI and still backed by server-side authorization.</td></tr>
  <tr><td></td><td>a. Are admin-only controls hidden from non-admin users?</td><td>&#9745;</td><td>&#9744;</td><td>&#9744;</td><td>Yes. Reviewed admin-only entry points and controls in files such as `frontend/app/admin/page.tsx`, `UserMenu.tsx`, and related app shells are gated by admin-role checks.</td></tr>
  <tr><td></td><td>b. Are group- or folder-scoped actions hidden when the user lacks permission?</td><td>&#9745;</td><td>&#9744;</td><td>&#9744;</td><td>Yes overall. `frontend/app/components/layouts/Sidebar.tsx` uses group/folder permission helpers to hide or disable actions when the current user lacks the required role.</td></tr>
  <tr><td></td><td>c. Is restricted functionality still protected on the backend, not only in the UI?</td><td>&#9745;</td><td>&#9744;</td><td>&#9744;</td><td>Yes. Backend auth and role/membership middleware such as `requireAdmin`, `authenticate`, and group-scoped permission checks still enforce restrictions independently of the frontend.</td></tr>

  <tr><td>58</td><td>Are form handling and client-side validation implemented appropriately?</td><td>&#9745;</td><td>&#9744;</td><td>&#9744;</td><td>Yes overall in the reviewed frontend/backend scope. Required-field, format, and server-side validation coverage is present across the representative auth/profile/task flows checked.</td></tr>
  <tr><td></td><td>a. Are required fields validated before submission?</td><td>&#9745;</td><td>&#9744;</td><td>&#9744;</td><td>Yes. Reviewed forms such as auth and sidebar creation flows check required inputs before submitting requests.</td></tr>
  <tr><td></td><td>b. Are invalid formats rejected clearly?</td><td>&#9745;</td><td>&#9744;</td><td>&#9744;</td><td>Yes overall. Auth-related forms clearly reject invalid email/password input and surface format-related validation feedback to the user.</td></tr>
  <tr><td></td><td>c. Are validation messages understandable to users?</td><td>&#9745;</td><td>&#9744;</td><td>&#9744;</td><td>Yes. The reviewed form flows display understandable validation/error messages rather than silent failures.</td></tr>
  <tr><td></td><td>d. Is server-side validation still enforced even when client-side validation exists?</td><td>&#9745;</td><td>&#9744;</td><td>&#9744;</td><td>Yes. Backend validation and service-level checks remain in place for auth, user, group, and task operations even when the frontend validates inputs first.</td></tr>

  <tr><td>59</td><td>Have i18n, formatting, and user preference settings been handled correctly?</td><td>&#9744;</td><td>&#9745;</td><td>&#9744;</td><td>Partially only. Shared preference contexts exist, but strings, date formatting, and language-preference handling are not applied consistently across the reviewed frontend.</td></tr>
  <tr><td></td><td>a. Are UI strings externalized where appropriate?</td><td>&#9744;</td><td>&#9745;</td><td>&#9744;</td><td>No. Many strings remain hard-coded outside the shared i18n layer, for example `frontend/app/admin/page.tsx` contains numerous inline English labels, and `frontend/app/components/AuthPages.tsx` keeps its own local translation table instead of using the shared app translation system consistently.</td></tr>
  <tr><td></td><td>b. Are date and time displays consistent with configured preferences?</td><td>&#9744;</td><td>&#9745;</td><td>&#9744;</td><td>No. Several frontend views still format dates/times directly with `toLocaleDateString()` / `toLocaleString()` instead of consistently routing through regional-preference helpers, for example in `frontend/app/admin/page.tsx`, `components/views/TasksView/ChecklistSection.tsx`, and `RepeatTaskModal.tsx`.</td></tr>
  <tr><td></td><td>c. Are language and theme preferences loaded safely?</td><td>&#9744;</td><td>&#9745;</td><td>&#9744;</td><td>Not consistently. Contexts such as `LanguageContext` and `RegionalContext` load preferences, but `frontend/app/components/AuthPages.tsx` keeps a separate local `language` state and translation map, so preference handling is fragmented rather than safely unified across the UI.</td></tr>

  <tr><td>60</td><td>Is the code aligned with the documented project architecture and module boundaries?</td><td>&#9744;</td><td>&#9745;</td><td>&#9744;</td><td>Mostly aligned at the folder-structure level, but not fully at the module-boundary level. The project follows the intended broad architecture while some large frontend components still accumulate too many cross-module responsibilities.</td></tr>
  <tr><td></td><td>a. Do backend modules follow the intended controller-service-model separation?</td><td>&#9745;</td><td>&#9744;</td><td>&#9744;</td><td>Yes overall. The backend generally follows a controller/service/model split across auth, task, group, notification, and admin modules.</td></tr>
  <tr><td></td><td>b. Do frontend modules follow the intended app/component/service structure?</td><td>&#9745;</td><td>&#9744;</td><td>&#9744;</td><td>Yes overall. The frontend broadly follows the expected separation into `app`, `components`, `services`, `contexts`, and supporting utility modules.</td></tr>
  <tr><td></td><td>c. Are cross-module dependencies kept reasonable and understandable?</td><td>&#9744;</td><td>&#9745;</td><td>&#9744;</td><td>No. Components such as `frontend/app/components/layouts/Sidebar.tsx` and `frontend/app/components/AuthPages.tsx` still pull in many contexts, services, permission helpers, and UI responsibilities at once, making their module boundaries harder to understand and maintain.</td></tr>
</table>

## Conclusion

- Fully Approved
- Conditionally Approved
- Rejected
- Discussion




