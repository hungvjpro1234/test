{
  "name": "todolist-unit-tests-frontend",
  "version": "1.0.0",
  "description": "Unit tests for Todo List App — frontend (Next.js/TypeScript)",
  "private": true,
  "scripts": {
    "test": "jest --config jest.config.ts --runInBand",
    "test:watch": "jest --config jest.config.ts --watch",
    "test:coverage": "jest --config jest.config.ts --coverage",
    "test:verbose": "jest --config jest.config.ts --verbose"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "jest": "^29.7.0",
    "ts-jest": "^29.3.2",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3"
  }
}
